############################################
# Munin Docker Stack - environment template #
############################################

# Timezone
TZ=UTC

# Public HTTP port (host:container)
NGINX_PORT_HTTP=80

# E-mail notifications (0=off, 1=on)
USE_MAIL_NOTIFICATIONS=0

# SMTP (used when USE_MAIL_NOTIFICATIONS=1)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASSWORD=change_me
SMTP_AUTH=on
SMTP_TLS=on
SMTP_STARTTLS=on
SMTP_FROM="Munin Bot <noreply@example.com>"

# Alert addresses (used when USE_MAIL_NOTIFICATIONS=1)
ALERT_FROM=noreply@example.com
ALERT_TO="ops@example.com"

# Munin nodes (space-separated name:address pairs)
# Examples:
#   by IP:   NODES="server1:10.0.0.101 server2:10.0.0.102"
#   by DNS:  NODES="db:db.internal.example.com web:web.internal.example.com"
NODES="server1:10.0.0.101 server2:10.0.0.102"
